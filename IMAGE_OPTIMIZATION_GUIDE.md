# 📸 ЭТАП 2: Оптимизация изображений - Инструкции

## ✅ Что уже сделано автоматически:

1. **LazyImage компонент улучшен:**
   - Добавлена поддержка WebP/AVIF форматов с fallback
   - Добавлены props `width` и `height` для предотвращения CLS
   - Добавлен `priority` режим для hero изображений
   - Blur placeholder для красивой загрузки

2. **ImageWithFallback улучшен:**
   - Добавлен `loading="lazy"` по умолчанию
   - Можно переопределить через props (например, `loading="eager"` для hero)

3. **Hero оптимизирован:**
   - Canvas grain effect отключен на мобильных (< 1024px)
   - Изображение прелоадится через JavaScript

---

## 🔧 Что нужно сделать ВРУЧНУЮ (с твоей помощью):

### Вариант 1: Конвертация изображений онлайн (РЕКОМЕНДУЕТСЯ)

1. **Экспортируй изображения из Figma:**
   - Hero image: `cfd9a9ca91e3e4b71d58c43a2eea6be171a94868.png`
   - Product images (gold/silver bars)
   - Logo: `6996fefb20a009345d2866832d3a8332840c6b74.png`

2. **Конвертируй через https://squoosh.app:**
   - Загрузи PNG
   - Выбери формат WebP (Quality: 80-85%)
   - Скачай конвертированный файл
   - Повтори для AVIF (Quality: 75-80%, еще лучше сжатие)

3. **Размести конвертированные файлы:**
   ```
   /src/assets/
     ├── hero.png (оригинал)
     ├── hero.webp (конвертированный)
     ├── hero.avif (конвертированный)
     └── ...
   ```

4. **Обнови импорты в коде:**
   ```tsx
   // Было:
   import heroImage from "figma:asset/cfd9a9ca91e3e4b71d58c43a2eea6be171a94868.png";
   
   // Стало:
   import heroImage from "@/assets/hero.png";
   import heroImageWebP from "@/assets/hero.webp";
   import heroImageAVIF from "@/assets/hero.avif";
   
   // Использование:
   <LazyImage
     src={heroImage}
     webpSrc={heroImageWebP}
     avifSrc={heroImageAVIF}
     width={1920}
     height={1080}
     priority
     alt="Goldiama Premium Gold"
   />
   ```

---

### Вариант 2: Vite Plugin (автоматическая конвертация)

Установи плагин для автоматической конвертации:

```bash
npm install -D vite-plugin-image-optimizer
```

**Обнови vite.config.ts:**
```typescript
import { ViteImageOptimizer } from 'vite-plugin-image-optimizer';

export default defineConfig({
  plugins: [
    react(),
    tailwindcss(),
    ViteImageOptimizer({
      png: { quality: 80 },
      jpeg: { quality: 80 },
      webp: { quality: 85 },
      avif: { quality: 75 }
    })
  ],
  // ...
});
```

---

## 📊 Приоритетные изображения для оптимизации:

1. **Hero image** (самый критичный):
   - Текущий размер: вероятно 2-4MB PNG
   - Целевой размер: ≤300-400KB WebP/AVIF
   - Размеры: 1920x1080 или 2560x1440 (не больше)

2. **Product images** (золото/серебро):
   - Оптимизировать до ≤150-200KB каждое
   - Добавить width/height для предотвращения CLS

3. **Logo** в preloader:
   - Оптимизировать до ≤50KB
   - Можно конвертировать в SVG для идеального качества

---

## 🎯 Ожидаемые результаты после конвертации:

- 📦 Размер изображений: **-60-70%**
- ⚡ First Contentful Paint: **-40-50%**
- 📉 CLS Score: **близко к 0** (благодаря width/height)
- 🚀 Lighthouse Performance: **+15-25 баллов**

---

## ⏭️ Следующий шаг:

Скажи мне когда будешь готов конвертировать изображения, или мы можем пропустить этот шаг и перейти к **ЭТАПУ 3** (CSS и эффекты) прямо сейчас!
