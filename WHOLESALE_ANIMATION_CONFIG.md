# 🎬 WHOLESALE PRODUCT CARDS - ANIMATION CONFIGURATION

**Полная документация всех анимаций и эффектов для карточек Wholesale секции**

---

## 📐 **РАЗМЕРЫ КАРТОЧЕК:**

```tsx
// Строка 244-247 в ProductSectionWholesale.tsx
height: 620px (mobile) / 720px (desktop)  // Gucci-inspired premium height
minWidth: 420px                           // Увеличено для более широкого формата ✅
maxWidth: 680px                           // Премиальная ширина ✅
borderRadius: 24px                        // Soft rounded corners
backgroundColor: #F5F5F3                  // Чуть темнее фона #FAFAF8
```

### Рекомендации по размерам:
- **Height:**
  - `520-580px` = компактные карточки
  - `620-720px` = Gucci-size ✅ текущее (премиальный формат)
  - `800+px` = editorial / magazine style

- **Width:**
  - `280-360px` = узкие карточки
  - `420-680px` = широкие карточки ✅ текущее (премиальный формат)
  - `700+px` = очень широкие (hero cards)

- **Aspect Ratio:**
  - Текущее: ~0.62-0.94 (более квадратные, широкие)
  - Portrait: 0.5-0.6 (узкие вертикальные)
  - Square: 1.0 (квадратные)

### Сравнение с индустрией:
```
Gucci Product Cards:     600-800px height, 400-600px width ✅ близко к нашему
Louis Vuitton:           500-700px height, 350-500px width
Tiffany & Co:            700-900px height, 500-700px width
Cartier:                 650-850px height, 450-650px width
```

**Текущий формат (420-680px × 620-720px) = премиальный, широкий, Gucci-inspired!**

---

## 📦 **1. ORBIT ANIMATION (Тонкая круговая орбита)**
**Всегда активна, работает в фоне**

```tsx
// Строка 75-76 в ProductSectionWholesale.tsx
const ORBIT_RADIUS = 8;      // px - радиус орбиты ✅ ОБНОВЛЕНО (было 3)
const ORBIT_SPEED = 0.015;   // скорость вращения ✅ ОБНОВЛЕНО (было 0.006)
```

### Рекомендации:
- **ORBIT_RADIUS:** 
  - `1-2` = едва заметно (zen)
  - `3-5` = subtle движение
  - `6-8` = заметное движение ✅ текущее
  - `10+` = очень активно

- **ORBIT_SPEED:**
  - `0.003` = очень медленно (медитативно)
  - `0.006` = медленно
  - `0.015` = средняя скорость ✅ текущее
  - `0.02+` = быстро

---

## 📈 **2. DRIFT ANIMATION (Вертикальный дрифт снизу вверх)**
**Всегда активна, медленное движение изображения**

```tsx
// Строка 102-104 в ProductSectionWholesale.tsx
const DRIFT_SPEED = 0.12;    // скорость движения вверх ✅ ОБНОВЛЕНО (было 0.045)
const TOP_BOUND = 5;         // верхняя граница (%)
const BOTTOM_BOUND = 95;     // нижняя граница (%, стартовая точка)
```

### Рекомендации:
- **DRIFT_SPEED:**
  - `0.02` = очень медленно (почти незаметно)
  - `0.045` = медленно
  - `0.08-0.12` = заметное движение ✅ текущее
  - `0.15+` = быстро (слишком динамично)

- **Границы:**
  - TOP_BOUND: `5%` = показывает верх продукта
  - BOTTOM_BOUND: `95%` = показывает низ продукта
  - Изображение циклически движется: 95% → 5% → 95% (loop)

---

## 🔍 **3. HOVER VERTICAL SWEEP (Макро-эффект при наведении)**
**Активируется только при hover**

```tsx
// Строка 144-146 в ProductSectionWholesale.tsx
const TOP = 5;               // верхняя точка sweep (%)
const BOTTOM = 95;           // нижняя точка sweep (%)
const BASE_SPEED = 3.5;      // базовая скорость движения ✅ ОБНОВЛЕНО (было 2.2)
const INTERVAL = 20;         // миллисекунды между кадрами ✅ ОБНОВЛЕНО (было 28, ~50fps)
```

### Рекомендации:
- **BASE_SPEED:**
  - `1.0-1.5` = медленный sweep (кинематографично)
  - `2.2` = средний sweep
  - `3.0-3.5` = быстрый sweep ✅ текущее (видимо как видео)
  - `4.0+` = очень быстрый (динамично)

- **INTERVAL:**
  - `16` = ~60fps (очень гладко, но нагрузка на CPU)
  - `20` = ~50fps ✅ текущее (плавно и видимо)
  - `28` = ~35fps (баланс)
  - `40` = ~25fps (менее гладко, но экономично)

### Easing настройки (строка 149-152):
```tsx
// Кубическая функция для плавного ускорения/замедления
const easeFactor = progress < 0.5
  ? 4 * progress * progress * progress           // ease-in-cubic
  : 1 - Math.pow(-2 * progress + 2, 3) / 2;     // ease-out-cubic
const speed = BASE_SPEED * (0.4 + 0.6 * easeFactor);
```
- `0.4` = минимальная скорость (40% от BASE_SPEED)
- `0.6` = диапазон ускорения (60% добавляется с easing)

---

## 🎯 **4. SCALE ANIMATION (Jump-Cut при hover)**
**⚡ ОБНОВЛЕНО: Теперь резкое переключение кадра!**

```tsx
// Строка 283-296 в ProductSectionWholesale.tsx
style={{
  transform: isHovered 
    ? 'scale(2.4)'   // Макро-план (увеличенный кадр)
    : 'scale(1)',    // Общий план
}}
transition={{
  scale: {
    duration: 0,                 // INSTANT - резкое переключение!
    ease: 'linear',
  }
}}
```

### Эффект "Jump-Cut" (Монтажный скачок):
```
┌─────────────────────────────────┐
│   ОБЩИЙ ПЛАН (Default)          │
│   Scale: 1.0                    │
│   Orbit drift animation         │
│                                 │
│   👆 HOVER                      │
│   ⚡ INSTANT CUT! (0ms)         │
│   👇                            │
│                                 │
│   МАКРО-ПЛАН (Hover)            │
│   Scale: 2.4                    │
│   Vertical sweep animation      │
└─────────────────────────────────┘
```

### Рекомендации:
- **Scale уровень:**
  - `1.8-2.0` = умеренный макро (видно больше контекста)
  - `2.4` = сильный макро ✅ текущее (cinematic close-up)
  - `3.0+` = экстремальный макро (очень близко)

- **Duration:**
  - `0` = мгновенный cut ✅ текущее (jump-cut эффект)
  - `0.05` = очень быстрый cut (~3 кадра при 60fps)
  - `0.1` = быстрый transition (заметно движение)

### Кинематографическая логика:
**ОБЩИЙ ПЛАН (Wide Shot):**
- Показывает весь продукт
- Медленный orbit drift (0.045 speed)
- Спокойное, созерцательное состояние

**↓ JUMP-CUT (0ms transition)**

**МАКРО-ПЛАН (Macro Close-up):**
- Увеличение 2.4x - видны детали
- Vertical sweep (2.2 speed) - изучение текстуры
- Bloom эффект - кинематографический glow
- Активное, исследовательское состояние

---

## ✨ **5. BLOOM GLOW (Свечение при hover)**

```tsx
// Строка 300-313 в ProductSectionWholesale.tsx
style={{
  background: 'radial-gradient(circle at 50% 50%, rgba(255, 255, 255, 0.15) 0%, transparent 70%)',
}}
animate={{
  opacity: isHovered ? 1 : 0,
}}
transition={{
  duration: 0.6,                        // секунды
  ease: [0.25, 0.46, 0.45, 0.94],      // quad ease-out
}}
```

### Рекомендации:
- **Opacity интенсивность:** `rgba(255, 255, 255, 0.15)`
  - `0.08` = едва заметное свечение
  - `0.15` = subtle свечение ✅ текущее
  - `0.25+` = сильное свечение (может быть слишком ярко)

- **Duration:**
  - `0.3` = быстрое появление
  - `0.6` = среднее ✅ текущее
  - `1.0` = медленное fade-in

---

## 🌾 **6. GRAIN OVERLAY (Текстура зерна)**

```tsx
// Строка 198-199 в ProductSectionWholesale.tsx
const grainDensity = 0.18;          // плотность зерна (0-1)
const resolutionMultiplier = 0.6;   // качество рендера (0-1)

// Строка 225-229
const brightness = 180 + Math.random() * 75;  // яркость зерна (0-255)
buffer[i + 3] = 180;                          // opacity зерна (0-255)
```

### Fade анимация:
```tsx
// Строка 330-338
animate={{
  opacity: isHovered ? 0 : 0.25,    // исчезает при hover
}}
transition={{
  opacity: {
    duration: 0.7,                   // секунды
    ease: [0.4, 0.0, 0.2, 1],       // material design easing
  }
}}
```

### Рекомендации:
- **grainDensity:**
  - `0.08-0.12` = легкое зерно
  - `0.18` = заметное зерно ✅ текущее
  - `0.25+` = сильное зерно (может быть грубо)

- **Opacity:**
  - `0.15` = едва заметно
  - `0.25` = subtle ✅ текущее
  - `0.4+` = очень заметно

---

## 💎 **7. CARD ENTRY ANIMATION (Появление карточки)**

```tsx
// Строка 265-272 в ProductSectionWholesale.tsx
initial={{ opacity: 0, y: 40, scale: 0.96 }}
whileInView={{ opacity: 1, y: 0, scale: 1 }}
transition={{ 
  duration: 1.2,                     // секунды
  ease: [0.19, 1.0, 0.22, 1.0],     // expo-out easing
  opacity: { 
    duration: 1.0,                   // секунды (отдельно для opacity)
    ease: 'easeOut' 
  }
}}
```

### Рекомендации:
- **Initial Y offset:**
  - `20` = небольшое движение
  - `40` = среднее ✅ текущее
  - `60` = большое движение (драматично)

- **Initial Scale:**
  - `0.92` = заметное масштабирование
  - `0.96` = subtle ✅ текущее
  - `0.98` = едва заметно

- **Duration:**
  - `0.8` = быстрое появление
  - `1.2` = среднее ✅ текущее (premium)
  - `1.5` = медленное (очень кинематографично)

---

## 📝 **8. TEXT OVERLAY ANIMATIONS**

### Initial fade-in:
```tsx
// Строка 348-351 в ProductSectionWholesale.tsx
initial={{ opacity: 0, y: 20 }}
whileInView={{ opacity: 1, y: 0 }}
transition={{ duration: 0.8, delay: 0.2 }}
```

### Hover brightness:
```tsx
// Строка 355-360 (Title)
animate={{ opacity: isHovered ? 1 : 0.95 }}
transition={{ duration: 0.4 }}

// Строка 369-374 (Meta)
animate={{ opacity: isHovered ? 1 : 0.85 }}
transition={{ duration: 0.4 }}

// Строка 390-395 (Category)
animate={{ opacity: isHovered ? 1 : 0.75 }}
transition={{ duration: 0.4 }}
```

### Рекомендации:
- **Default opacity:**
  - Title: `0.95` = почти полная ✅
  - Meta: `0.85` = немного приглушено ✅
  - Category: `0.75` = приглушено ✅

- **Hover transition:**
  - `0.3` = быстрое изменение
  - `0.4` = среднее ✅ текущее
  - `0.6` = медленное

---

## 🎨 **ВИЗУАЛЬНЫЕ ЭФФЕКТЫ:**

### Vignette (виньетка):
```tsx
// Строка 316-321
background: 'radial-gradient(circle at 50% 50%, transparent 50%, rgba(0, 0, 0, 0.08) 100%)'
```
- `rgba(0, 0, 0, 0.08)` = интенсивность затемнения краев
- `50%` = начало затемнения (от центра)

### Text gradient overlay:
```tsx
// Строка 346-348
background: 'linear-gradient(to top, rgba(0, 0, 0, 0.75) 0%, rgba(0, 0, 0, 0.4) 60%, transparent 100%)'
```
- `0.75` = непрозрачность внизу (читаемость)
- `0.4` = непрозрачность в середине
- `60%` = высота градиента

---

## 🎯 **БЫСТРЫЕ ПРЕСЕТЫ:**

### Preset 1: **ZEN MODE** (очень медленно, медитативно)
```tsx
ORBIT_RADIUS = 2
ORBIT_SPEED = 0.003
DRIFT_SPEED = 0.03
BASE_SPEED = 1.5
INTERVAL = 40
SCALE = 2.0
```

### Preset 2: **BALANCED** (баланс премиальности)
```tsx
ORBIT_RADIUS = 3
ORBIT_SPEED = 0.006
DRIFT_SPEED = 0.045
BASE_SPEED = 2.2
INTERVAL = 28
SCALE = 2.4
```

### Preset 3: **VIDEO-LIKE** (видимое движение, как видео) ✅ ТЕКУЩЕЕ
```tsx
ORBIT_RADIUS = 8
ORBIT_SPEED = 0.015
DRIFT_SPEED = 0.12
BASE_SPEED = 3.5
INTERVAL = 20
SCALE = 2.4
```

### Preset 4: **DYNAMIC** (очень активно)
```tsx
ORBIT_RADIUS = 10
ORBIT_SPEED = 0.02
DRIFT_SPEED = 0.18
BASE_SPEED = 4.5
INTERVAL = 16
SCALE = 2.8
```

### Preset 5: **CINEMATIC** (медленно, драматично)
```tsx
ORBIT_RADIUS = 2
ORBIT_SPEED = 0.004
DRIFT_SPEED = 0.035
BASE_SPEED = 1.8
INTERVAL = 30
SCALE = 2.6
duration: 1.0 (все transitions)
```

---

## 📍 **ГДЕ НАЙТИ В КОДЕ:**

| Анимация | Строки | Параметры |
|----------|--------|-----------|
| **Orbit** | 75-76 | ORBIT_RADIUS, ORBIT_SPEED |
| **Drift** | 102-104 | DRIFT_SPEED, TOP_BOUND, BOTTOM_BOUND |
| **Hover Sweep** | 144-146 | TOP, BOTTOM, BASE_SPEED, INTERVAL |
| **Scale** | 283-296 | scale, duration, ease |
| **Bloom** | 300-313 | opacity, duration, gradient |
| **Grain** | 198-199, 330-338 | grainDensity, opacity |
| **Entry** | 265-272 | y, scale, duration |
| **Text** | 348-395 | opacity, duration, delay |

---

## 🛠️ **КАК НАСТРОИТЬ:**

1. Откройте `/src/app/components/ProductSectionWholesale.tsx`
2. Найдите нужную секцию по номеру строки
3. Измените значение константы
4. Сохраните и посмотрите результат

### Примеры изменений:

**Сделать orbit более заметным:**
```tsx
// Строка 75
const ORBIT_RADIUS = 5;  // было 3
```

**Замедлить drift:**
```tsx
// Строка 102
const DRIFT_SPEED = 0.03;  // было 0.045
```

**Увеличить zoom при hover:**
```tsx
// Строка 284
transform: isHovered ? 'scale(3.0)' : 'scale(1)',  // было 2.4
```

---

## ⚡ **ПРОИЗВОДИТЕЛЬНОСТЬ:**

### Оптимально:
- ✅ 4 карточки одновременно
- ✅ 60fps canvas grain
- ✅ RequestAnimationFrame для orbit/drift
- ✅ Motion library для остальных анимаций

### Если тормозит:
1. Уменьшить `grainDensity` до 0.12
2. Уменьшить `resolutionMultiplier` до 0.4
3. Увеличить `INTERVAL` до 40

---

**Все настройки в одном месте! Экспериментируйте! 🎨✨**

---

## 🎬 **НОВИНКА: JUMP-CUT ЭФФЕКТ**

### Что это?
**Jump-Cut** - кинематографический прием монтажа, где происходит резкое переключение между кадрами без плавного перехода. Как будто камера мгновенно "перепрыгнула" из одной позиции в другую.

### Визуализация эффекта:

```
БЫЛО (старая версия - плавный zoom):
┌──────────────────────────────────────────┐
│  ════════════════════════════════════     │
│  Scale: 1.0 ─────→ 1.5 ─────→ 2.0 ─────→ 2.4  │
│  0ms        200ms      500ms      850ms   │
│  [Плавное увеличение за 0.85 секунды]    │
└──────────────────────────────────────────┘
```

```
СТАЛО (новая версия - jump-cut):
┌──────────────────────────────────────────┐
│  ⚡                                        │
│  Scale: 1.0 ━━━━━━━━━━━━━━━━━━━━━━━━━→ 2.4  │
│  0ms                                0ms   │
│  [МГНОВЕННОЕ переключение кадра!]        │
└──────────────────────────────────────────┘
```

### Как это работает:

**1. DEFAULT STATE (Общий план):**
```
┌─────────────────────────┐
│                         │
│   [Full Product View]   │
│                         │
│   • Scale: 1.0          │
│   • Orbit animation     │
│   • Drift: 95% → 5%     │
│   • Grain: visible      │
│                         │
└─────────────────────────┘
```

**2. HOVER (Мгновенное переключение):**
```
👆 CLICK / HOVER

⚡⚡⚡ INSTANT CUT (0ms) ⚡⚡⚡

┌─────────────────────────┐
│  ┌───────────────────┐  │
│  │                   │  
│  │  [Macro Details]  │  │
│  │                   │  │
│  │  • Scale: 2.4     │  │
│  │  • Sweep: active  │  │
│  │  • Bloom: glow    │  │
│  │  • Grain: hidden  │  │
│  │                   │  │
│  └───────────────────┘  │
└─────────────────────────┘
```

**3. UNHOVER (Возврат):**
```
👆 MOUSE OUT

⚡⚡⚡ INSTANT CUT BACK (0ms) ⚡⚡⚡

┌─────────────────────────┐
│                         │
│   [Full Product View]   │
│   Возврат к общему плану│
│                         │
└─────────────────────────┘
```

### Кинематографические примеры:

**Фильм "Breathless" (1960) - Jean-Luc Godard:**
Пионер jump-cut техники. Использовал резкие скачки во времени и пространстве.

**Apple Product Videos:**
Часто используют резкое переключение между общим планом продукта и макросъемкой деталей.

**Gucci/Dior Fashion Films:**
Jump-cuts между wide shots и extreme close-ups тканей, текстур, деталей.

---

## 🎯 **НАСТРОЙКА JUMP-CUT ЭФФЕКТА:**

### Опция 1: INSTANT (текущий) ✅
```tsx
duration: 0,
ease: 'linear',
```
**Результат:** Мгновенное переключение (0ms)  
**Ощущение:** Резкий монтажный срез, editorial style

### Опция 2: NEAR-INSTANT
```tsx
duration: 0.05,  // 50ms = ~3 кадра при 60fps
ease: 'linear',
```
**Результат:** Почти мгновенное (3-4 кадра)  
**Ощущение:** Быстрый cut с микро-блюром

### Опция 3: QUICK-CUT
```tsx
duration: 0.1,  // 100ms = ~6 кадров
ease: 'linear',
```
**Результат:** Быстрое переключение (заметное движение)  
**Ощущение:** Aggressive zoom, энергичный

### Опция 4: SMOOTH (старая версия)
```tsx
duration: 0.85,
ease: [0.33, 1, 0.68, 1],
```
**Результат:** Плавный zoom за 0.85 секунды  
**Ощущение:** Кинематографический dolly-in

---

## 🎨 **ВАРИАНТЫ SCALE ДЛЯ JUMP-CUT:**

| Scale | Эффект | Применение |
|-------|--------|-----------|
| `1.5` | Легкий макро | Subtle, показывает больше контекста |
| `2.0` | Средний макро | Balanced, комфортный для глаз |
| `2.4` | Сильный макро ✅ | Dramatic, видны детали текстуры |
| `3.0` | Экстремальный | Extreme close-up, абстрактные формы |
| `4.0` | Ультра-макро | Artistic, очень близко |

---

## 🎭 **СОЧЕТАНИЯ С ДРУГИМИ ЭФФЕКТАМИ:**

### Вариант A: **Pure Jump-Cut** (минималистичный)
```tsx
SCALE: 2.4
DURATION: 0
BLOOM: disabled (opacity: 0)
GRAIN: keep visible
SWEEP: slower (BASE_SPEED: 1.5)
```
**Стиль:** Raw, documentary, editorial

### Вариант B: **Cinematic Jump-Cut** (текущий) ✅
```tsx
SCALE: 2.4
DURATION: 0
BLOOM: enabled (opacity: 0.15)
GRAIN: fade out on hover
SWEEP: medium (BASE_SPEED: 2.2)
```
**Стиль:** Premium, luxury, fashion film

### Вариант C: **Aggressive Jump-Cut** (динамичный)
```tsx
SCALE: 3.0
DURATION: 0
BLOOM: strong (opacity: 0.25)
GRAIN: instant hide
SWEEP: fast (BASE_SPEED: 3.5)
```
**Стиль:** Editorial, high-energy, modern

### Вариант D: **Dreamy Jump-Cut** (мягкий)
```tsx
SCALE: 2.0
DURATION: 0.05
BLOOM: very subtle (opacity: 0.08)
GRAIN: slow fade (duration: 1.0)
SWEEP: slow (BASE_SPEED: 1.2)
```
**Стиль:** Ethereal, soft, romantic

---

## 📊 **TIMING COMPARISON:**

```
INSTANT (0ms):
Frame 0:  [Wide Shot]
Frame 1:  [Macro Shot] ← Immediate
─────────────────────────────────
Perception: Jarring, editorial

QUICK (50ms / ~3 frames):
Frame 0:  [Wide Shot]
Frame 1:  [Transitioning...]
Frame 2:  [Transitioning...]
Frame 3:  [Macro Shot]
─────────────────────────────────
Perception: Fast but smooth edge

SMOOTH (850ms / ~51 frames):
Frame 0:  [Wide Shot]
Frame 10: [Scale: 1.3]
Frame 20: [Scale: 1.6]
Frame 30: [Scale: 1.9]
Frame 40: [Scale: 2.2]
Frame 51: [Macro Shot]
─────────────────────────────────
Perception: Cinematic dolly-in
```

---

## 🎥 **INDUSTRY EXAMPLES:**

### Fashion E-Commerce:
- **Gucci.com:** Jump-cuts между product shots
- **Net-a-Porter:** Instant transitions на PDP
- **Farfetch:** Quick-cuts в video lookbooks

### Luxury Brands:
- **Rolex:** Instant macro cuts для watch details
- **Hermès:** Editorial jump-cuts в product films
- **Cartier:** Резкие переходы к jewelry close-ups

### Apple:
- **Product launches:** Aggressive cuts между планами
- **Material focus:** Instant jumps к texture shots
- **Feature demos:** Quick transitions для динамики

---

## 🛠️ **КАК ИЗМЕНИТЬ:**

**Файл:** `/src/app/components/ProductSectionWholesale.tsx`  
**Строка:** 289-295

```tsx
transition={{
  scale: {
    duration: 0,        // ← Измените это!
    ease: 'linear',     // ← Или ��то для плавности
  }
}}
```

### Примеры:

**Вернуться к плавному zoom:**
```tsx
duration: 0.85,
ease: [0.33, 1, 0.68, 1],
```

**Очень быстрый cut:**
```tsx
duration: 0.03,  // ~2 кадра
ease: 'linear',
```

**Aggressive jump с expo-easing:**
```tsx
duration: 0.05,
ease: [0.9, 0, 0.1, 1],  // Резкий старт
```

---

## 💡 **РЕКОМЕНДАЦИИ:**

### Используйте Jump-Cut если:
✅ Хотите editorial/fashion feel  
✅ Нужна высокая энергетика  
✅ Важна скорость взаимодействия  
✅ Минималистичный, современный стиль

### НЕ используйте Jump-Cut если:
❌ Нужна плавность и комфорт  
❌ Аудитория пожилая/консервативная  
❌ Продукт требует gentle presentation  
❌ Бренд традиционный, классический

---

## 🎯 **ТЕКУЩАЯ КОНФИГУРАЦИЯ:**

```tsx
// ProductSectionWholesale.tsx
SCALE: 2.4           // Strong macro
DURATION: 0          // INSTANT jump-cut
EASE: 'linear'       // No easing
BLOOM: 0.15          // Subtle glow
GRAIN: fade out      // Cleaner on macro
SWEEP: 2.2           // Medium speed
```

**Результат:**  
⚡ Резкое переключение между общим планом и макросъемкой  
🎬 Кинематографический editorial feel  
💎 Premium luxury aesthetic  
⚡ Мгновенная реакция на hover

---

**Jump-Cut эффект активирован! Почувствуйте кинематографическую магию! 🎬✨**